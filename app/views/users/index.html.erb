<p id="notice"><%= notice %></p>

<h1>Liste des utilisateurs</h1>

<table>


    <% @users.each do |user| %>
	    
	    <div class="vignette">
	      	
	        <%= user.username %>
	       
	       
	        <%= link_to image_tag(user.img_url), user_twaats_path(user) %>
	
	      <div>

	        <%= link_to 'Edit', edit_user_path(user),class: "myButton" %>
</div>
	        <%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }, class: "myButton" %>
	    </div>
    <% end %>

<br>
<div class="btn" >
<%= link_to 'Create a new user', new_user_path %>	
</div>

